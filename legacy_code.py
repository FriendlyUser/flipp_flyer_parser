# An attempt to grab the flyer image, but whatever, will stick with urls and images from aside
# Get the position and size of the button
# location = calc_location_styles_from_button(button)
# viewport_width = driver.execute_script("return window.innerWidth")
# width = (72 / 100) * viewport_width
# left_px = (location.get('left') / 100) * viewport_width
# top_px = location.get('top')
# right_px = location.get('left') + width
# bottom_px = top_px + location.get('height')
# # Crop the screenshot based on the button's position and size
# screenshot = Image.open("data/full_screenshot.png")
# cropped_image = screenshot.crop((left_px, top_px, right_px, bottom_px))
# cropped_image.save("data/cropped_screenshot.png")